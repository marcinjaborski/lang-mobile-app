import { LeaderboardsScreenProps } from "@src/features/studying";
import { SafeAreaView } from "react-native-safe-area-context";

export const LeaderboardsScreen = ({}: LeaderboardsScreenProps) => {
  return <SafeAreaView />;
};
