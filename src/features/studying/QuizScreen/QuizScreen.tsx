import { QuizScreenProps } from "@src/features/studying";
import { SafeAreaView } from "react-native-safe-area-context";

export const QuizScreen = ({}: QuizScreenProps) => {
  return <SafeAreaView />;
};
